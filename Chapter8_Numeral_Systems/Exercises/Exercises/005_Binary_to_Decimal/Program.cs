/* Write a program that converts a binary number to decimal one.*/

void convert_binary_to_decimal(int number)
{
    Console.Write(number);
    Console.WriteLine();
}
convert_binary_to_decimal(0b0110);